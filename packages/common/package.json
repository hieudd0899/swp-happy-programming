{
    "name": "common",
    "type": "module",
    "scripts": {
        "generate": "npx prisma generate",
        "push": "npx prisma db push",
        "lint": "eslint ."
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}
